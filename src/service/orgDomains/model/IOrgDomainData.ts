import type { IDomain } from './IDomain'
import type { IOrganisationDomain } from './IOrganisationDomain'

export interface IOrgDomainData {
  allDomains: IDomain[]
  organisationDomains: IOrganisationDomain[]
}
